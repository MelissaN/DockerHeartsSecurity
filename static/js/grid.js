/* This class handles room collision.

	It contains a barrier matrix
	Character decides whether to update, by checking room matrix
		I suppose characters should contain info about the room they're in

	formula:
		matrix must be of size canvas
		character must initialize to a valid spot in the matrix
		character passes in current values, which get corrected by path check function
			startx and starty - only for user's character
*/

class Grid{

	
}

